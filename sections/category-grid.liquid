<section class="category-grid">
  <div class="container">
    <div class="categories">
      <div class="category-card">
        <div class="category-image">
          {% if section.settings.category_1_image %}
            <img src="{{ section.settings.category_1_image | img_url: '400x300' }}" alt="Best Sellers" />
          {% else %}
            <div class="category-placeholder">Best Sellers Image</div>
          {% endif %}
        </div>
        <h3 class="category-title">Best Sellers</h3>
      </div>

      <div class="category-card">
        <div class="category-image">
          {% if section.settings.category_2_image %}
            <img src="{{ section.settings.category_2_image | img_url: '400x300' }}" alt="Fragrances for Her" />
          {% else %}
            <div class="category-placeholder">Fragrances for Her Image</div>
          {% endif %}
        </div>
        <h3 class="category-title">Fragrances for Her</h3>
      </div>

      <div class="category-card">
        <div class="category-image">
          {% if section.settings.category_3_image %}
            <img src="{{ section.settings.category_3_image | img_url: '400x300' }}" alt="Mists & Lotions" />
          {% else %}
            <div class="category-placeholder">Mists & Lotions Image</div>
          {% endif %}
        </div>
        <h3 class="category-title">Mists & Lotions</h3>
      </div>

      <div class="category-card">
        <div class="category-image">
          {% if section.settings.category_4_image %}
            <img src="{{ section.settings.category_4_image | img_url: '400x300' }}" alt="Bath & Body" />
          {% else %}
            <div class="category-placeholder">Bath & Body Image</div>
          {% endif %}
        </div>
        <h3 class="category-title">Bath & Body</h3>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Category Grid",
  "settings": [
    {
      "type": "image_picker",
      "id": "category_1_image",
      "label": "Best Sellers Image"
    },
    {
      "type": "image_picker",
      "id": "category_2_image",
      "label": "Fragrances for Her Image"
    },
    {
      "type": "image_picker",
      "id": "category_3_image",
      "label": "Mists & Lotions Image"
    },
    {
      "type": "image_picker",
      "id": "category_4_image",
      "label": "Bath & Body Image"
    }
  ]
}
{% endschema %}
